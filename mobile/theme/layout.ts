/** Default hero band height (about, settings, notifications, sensor). */
export const HERO_MIN_HEIGHT = 200;

/** Profile tab uses a taller hero for avatar overlap. */
export const HERO_MIN_HEIGHT_PROFILE = 350;
